# Issue 24: PR #24 actionable feedback

## Remediation

- Keep relative-path provenance while writing flat, collision-safe artifacts.
- Drain streamed stdout and stderr concurrently.
- Report entity-extraction progress in processed documents, not candidate rows.
- Rerun pull-request governance when pull-request metadata, including its base branch, is edited.
- Add regression coverage for nested paths, failure logs, streaming output, and progress counts.

## Validation

The standard test, coverage, pylint, mypy, compile, and whitespace checks must pass before merge.
